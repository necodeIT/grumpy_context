name: grumpy_context
description: Analyze Dart and Flutter project structure for grumpy-aware bricks and lints.
version: 1.0.0

environment:
  sdk: ^3.11.4

dependencies:
  logging: ^1.3.0
  path: ^1.9.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
